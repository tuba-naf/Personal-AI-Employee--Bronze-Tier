[project]
name = "ai-employee-bronze"
version = "0.1.0"
description = "Bronze Tier Personal AI Employee - Content Draft Generation"
requires-python = ">=3.13"
dependencies = [
    "watchdog>=4.0.0",
    "feedparser>=6.0.0",
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
